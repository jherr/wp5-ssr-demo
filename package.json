{
  "name": "icarus",
  "description": "React architecture at scale",
  "version": "1.0.0",
  "main": "server/index.js",
  "author": "Zack Jackson <zack@ScriptedAlchemy.com>",
  "license": "MIT",
  "scripts": {
    "build": "concurrently \"cd website1; npm install && npm run build\" \"cd website2; npm install && npm run build\"",
    "serve": "concurrently \"cd website1; npm run serve\" \"cd website2; npm run serve\""
  },
  "dependencies": {
    "concurrently": "^5.1.0"
  }
}
